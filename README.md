# optimum-issue-repro
Example of a bug we're hitting with when using Optimum to export Moonshine models to Onnx

```bash
pip install -r requirements.txt
```

```bash
./download-moonshine-model.sh base tiny
```